import { cleanup } from "@testing-library/react";

afterEach(cleanup);

describe("App component", () => {
  it("should render the main app without any issue", () => {
    // render(<App />);
  });
});
