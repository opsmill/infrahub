import type { EmptyFieldValue } from "@/shared/components/form/type";

export const DEFAULT_FORM_FIELD_VALUE: EmptyFieldValue = {
  source: null,
  value: null,
};

export const RELATIONSHIP_BULK_ADD_PREFIX = "__add_";
export const RELATIONSHIP_BULK_REMOVE_PREFIX = "__remove_";
