---
overwrite: true
schema: "http://localhost:8000/graphql"
documents: "./src/graphql/queries/*.ts"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
    config:
      withHooks: true
