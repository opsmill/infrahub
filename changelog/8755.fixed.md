Users with the default "General Access" role can now create, update, comment on, and discuss proposed changes without an administrator granting them the global "edit default branch" permission, restoring the contributor workflow on out-of-the-box installations. Existing installations are updated automatically on upgrade via a graph migration that backfills the missing role permissions.
