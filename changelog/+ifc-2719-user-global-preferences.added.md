Added schema-backed user and global preferences: `CoreGlobalPreference` holds organisation-wide defaults (`date_format`, `timezone`) as a singleton seeded at initialization (graph migration backfills existing installs), `CoreUserPreference` stores per-account overrides, and the new `InfrahubEffectivePreferences` GraphQL query returns the merged effective values for the calling user.
