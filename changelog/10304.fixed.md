An SSO user whose account was deleted in Infrahub can sign in again. The next login now creates a new account, like a first login, instead of failing with an error. Deleting an account also removes its external identities and its API tokens, which were left behind before.
