An SSO user's account can be deleted without leaving broken data behind. Deleting an account now also removes its external identities, its API tokens and its refresh tokens, which were left behind before. The ones left by an earlier release are removed on upgrade, and a login that meets one reports a clear error instead of failing with an unhandled server error.
