In Account settings → Preferences, the date format and timezone fields now preview what saving would actually produce. Clearing a personal override shows the organisation default that takes over — previously the date-format `Example:` fell back to your browser's timezone, and disappeared altogether when the date format itself was cleared. The information icon now describes the value you are about to save rather than the one already stored, so it no longer names a personal preference you have just cleared. To make this possible, `InfrahubEffectivePreferences` now returns an `inherited` layer alongside each field's `value` and `source`, reporting what the field would resolve to if the caller set nothing of their own.
