The date-format `Example:` preview in Account settings → Preferences now renders in the timezone that will actually apply after saving, including while a personal timezone override is being cleared and an organisation default exists. The (i) source tooltip now names the organisation default that a personal preference overrides, and describes only where the saved value comes from rather than rendering a sample in an unsaved timezone.
