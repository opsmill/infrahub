Long values in a Text attribute are now fully readable in create and edit forms: the field wraps and grows with its content instead of clipping the value at its right edge. Enter still submits the form and pasted newlines are stripped, matching the previous single-line behavior.
