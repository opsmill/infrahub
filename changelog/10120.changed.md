Changed the Neo4j connection pool size per process from the driver's implicit default of 100 to 50. Deployments that relied on the previous ceiling can restore it by setting `INFRAHUB_DB_MAX_CONNECTION_POOL_SIZE=100`.
