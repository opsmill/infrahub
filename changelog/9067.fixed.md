Fixed task list, task details, and tasks homepage pages firing one GraphQL query per related node, which could freeze or crash the browser on tasks with thousands of related nodes. Display labels are now resolved through a single batched query per page.
