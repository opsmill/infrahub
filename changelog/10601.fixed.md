Fixed the schema load and check endpoints rejecting an invalid schema with a single error covering the whole schema: the 422 response now carries one entry per offending field, located on that field and carrying the value it received.
