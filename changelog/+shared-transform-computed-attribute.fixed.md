A Python computed attribute that shares its transform with another computed attribute is now recomputed automatically. The automations were indexed by transform name, so out of several attributes fed by one transform only one got an automation. The others were never refreshed when a node changed and kept the value of their last successful compute. Each attribute now gets its own automation, both for a change to a node of its own kind and for a change to a node its transform query reads.
