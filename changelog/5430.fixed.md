Fixed a GraphQL query that selects the same top-level field more than once (for example `BuiltinTag { count }` alongside `BuiltinTag { edges { ... } }`) returning only the first occurrence's selection and dropping the rest. When a dropped selection held a non-null field the whole response failed with a "Cannot return null for non-nullable field" error. The selections from every occurrence are now merged into one result.
