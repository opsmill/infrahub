The bug pipeline can now use the E2E test tier: a CI proof job on pipeline PRs verifies the reproduction fails on its assertion before the fix and passes after it, embeds before/after screenshots into the PR description for frontend bugs, and the fix phase demotes the reproduction to the cheapest adequate test tier (or keeps it under tests/e2e/regressions/ when only the full stack can express it).
