Python computed attributes no longer recompute on every schema change when their transform query reads a human-friendly id or display label. Such a read marked the whole read set imprecise, so a change to an unrelated kind still recomputed the attribute on every node of its kind. A derived value built from the kind's own attributes is now scoped to that kind; one whose definition crosses a relationship still recomputes on any schema change.
