Modified event filters for mandatory computed attributes to not include the infrahub.node.created event as the computed attribute will be rendered on node creation regardless. This change will avoid extra processing in the background workers.
