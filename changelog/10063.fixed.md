Fixed a repository's first import failing when its objects declared generator actions or trigger rules referencing a generator definition from the same repository; those objects are now imported after the definitions they depend on, so the import succeeds on an empty instance without a manual reload.
