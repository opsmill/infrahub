Fixed silently recording a branch merge in an external repository when the remote rejected the push (for example on a protected branch); the merge now fails with an error describing the rejection and can be retried once push access is restored.
