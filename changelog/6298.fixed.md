Fixed a branch merge in an external repository being recorded in Infrahub even when the remote rejected the push (for example on a protected branch); the merge now fails with an error describing why the remote refused it, and the repository is left unchanged instead of ahead of the remote.
