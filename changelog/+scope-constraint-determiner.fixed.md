Schema constraint validation triggered by a data change now runs only the node-level constraints (uniqueness and hierarchy) whose specific field or path was actually modified, instead of every node-level constraint defined on the changed kind. A change to a non-unique attribute no longer re-validates a kind's uniqueness constraints, reducing the work performed during proposed-change checks, merges, and rebases.
