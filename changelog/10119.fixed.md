Creating an object now retries transient database errors the same way updates, upserts, and deletes already do, instead of surfacing a raw Neo4j "there are no available threads to serve this request" message when the database is briefly saturated.
