Merging two branches that each introduced the same unique value could leave duplicate values on the destination branch. A proposed change validated its schema and data integrity earlier in its pipeline but did not re-check them at merge time, so a violation introduced by another branch merged in the meantime was missed. Proposed changes now re-run their schema and data integrity checks immediately before merging. The proposed change schema-integrity check also now validates relationship constraints, which were previously skipped.
