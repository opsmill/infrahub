Python computed attributes no longer recompute on every schema change when their query follows a relationship to a generic. The analyzer reports every member kind of the generic, and the members the query reads no field from came back empty, which marked the whole read set as imprecise. Such a kind is now kept as a kind-level dependency only, so adding or removing it still triggers a recompute while a field change on it does not.
