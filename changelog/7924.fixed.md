Required computed attributes are now enforced as unique on create, preventing duplicate values (and duplicate human-friendly IDs) from being saved. The constraint violation message now also lists the input attributes a computed attribute depends on, so the value to change is obvious.
