Added `INFRAHUB_WORKFLOW_CONCURRENCY_LIMIT_ARTIFACT_GENERATE` (default `10`) to cap concurrent runs of the `artifact-generate` workflow at the Prefect deployment level. Set to `0` to disable the cap.
