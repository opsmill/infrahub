The TRY400 (error-instead-of-exception) ruff rule is now enforced — a `log.error` reporting a caught exception is either `log.exception`, so the traceback reaches the logs, or carries an explicit justified `# noqa: TRY400`. Nine now-redundant `# noqa: BLE001` suppressions were dropped as a result.
