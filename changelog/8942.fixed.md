Fix Search Anywhere crashing when searching for a Schema or Internal node UUID (for example a `SchemaNode` UUID coming from a proposed change validation error). These results are now returned with a display label and resolve to the relevant schema page instead of crashing the UI.
