Added the `manage_global_preferences` global permission. The `InfrahubSetPreferences` mutation and the `InfrahubGlobalPreferences` query enforce it before touching the organisation-wide row when `scope` is `GLOBAL` (super admins implicitly pass). Writing or reading a user's own preferences (`scope: USER`) needs no permission — those paths only ever touch the calling account's own row.
