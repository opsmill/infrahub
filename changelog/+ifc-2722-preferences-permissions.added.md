Added the `manage_global_preferences` global permission: `InfrahubGlobalPreferenceUpdate` enforces it imperatively before writing the singleton (super admins implicitly pass). User preferences need no permission — `InfrahubUserPreferenceUpsert` only ever writes the calling account's own row. The effective query also returns a `can_edit_global_preferences` flag for frontend tab gating.
