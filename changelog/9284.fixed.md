When an existing node kind starts inheriting from a generic, the attributes it gains are now created on the kind's pre-existing nodes, including their Profile and Template instances, with NumberPool attributes allocated from the generic's pool. Kind-update migrations now run to completion before any other schema migration so they can no longer race one another.
