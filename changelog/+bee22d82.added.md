Added ability to delete a branch both in infrahub and in git also if synched with git. Controlled by 2 environment variables `delete_branch_after_merge` and `delete_git_branch_after_merge` which default to False
