Added the `InfrahubRepositoryBranchStatus` GraphQL query, returning one row per branch with the repository's `commit`, `sync_status`, `internal_status`, and `ref` as that branch resolves them, filterable with `name__value`, `partial_match`, `status__value`, `sync_status__value`, `internal_status__value`, and `own_values_only`.
