Removing an attribute from a generic no longer leaves stale references on inheriting nodes. `cleanup_inherited_elements` now strips paths pointing at deleted attributes or relationships from each inheritor's `uniqueness_constraints`, `human_friendly_id`, `order_by`, `display_labels`, and `default_filter`. Previously the orphaned references caused the post-write schema reload to raise `ValueError` and return HTTP 500.
