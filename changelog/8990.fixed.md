Fix schema load failing when removing an attribute from a generic that an inheriting node still referenced in its `human_friendly_id`, `uniqueness_constraints`, `order_by`, `display_labels`, or `default_filter`. Those stale references on the inheriting node are now cleaned up automatically as part of the load.
