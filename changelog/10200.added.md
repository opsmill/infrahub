`InfrahubEffectivePreferences` now returns an `inherited` layer alongside each field's resolved `value` and `source`, reporting what that field would resolve to if the caller set nothing of their own. Available to any authenticated caller, so a client can show the organisation default a personal override is shadowing without needing the `manage_global_preferences` permission.
