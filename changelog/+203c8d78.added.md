Added login, failed login, and logout activity events (`infrahub.account.logged_in`, `infrahub.account.login_failed`, `infrahub.account.logged_out`). Successful and failed authentication attempts via password, OAuth2, and OIDC, as well as explicit logouts, are now queryable via the activity event feed and can trigger existing webhook integrations.
