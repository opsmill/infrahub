The web frontend is now served through FastAPI's native `app.frontend()` support instead of hand-rolled static mounts and a Jinja2 single-page-app catch-all route. Unknown non-navigation paths (for example an unknown `/api/*` path requested as JSON) now return a real `404` instead of the SPA shell with a `200`.
