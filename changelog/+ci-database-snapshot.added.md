Added `infrahub db snapshot-dump` and `infrahub db snapshot-restore` commands that capture the initialized database as a portable JSON graph and replay it with bulk `CREATE` statements. Restoring a snapshot into an empty database is a fast, portable alternative to running the full first-time initialization (no `neo4j-admin` dump, no binary/version coupling), primarily used to speed up test/CI database bootstrap.
