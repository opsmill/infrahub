# Critique Report: Priority Work Queue Foundation for the Task Worker

**Date**: 2026-07-02
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Verdict**: ✅ PROCEED

---

## Executive Summary

The spec and plan are unusually well-grounded: the PRD came out of an idea-grilling session against the codebase, and the plan's research verified every load-bearing Prefect 3.7.5 behavior against the installed package source (missing-queue auto-create, priority bumping, worker all-queue polling) rather than assuming it. The zero-behavior-change scope is crisp, the YAGNI exception is honestly tracked, and the fallback design was chosen specifically to be testable without mocks. No must-address findings. The recommendations below tighten operational observability, the downgrade story, one brittle test assertion, and an enterprise-compatibility check — all small.

---

## Product Lens Findings 🎯

### Problem Validation

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | 💡 | Value in this slice is entirely developer/operator-facing (a seam); end-user value arrives only with follow-up INFP-635 slices. The justification (committed follow-up work) lives in Assumptions and Complexity Tracking — good — but the PR description is the last gate where a reviewer can challenge it. | Keep the PRD's instruction to restate the YAGNI rationale in the PR description; make it an explicit task in tasks.md so it isn't dropped. |

### User Value Assessment

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P2 | ✅ | User stories are honest about who benefits (developer, operator, end user "nothing changes"). Story 4 (UI visibility) correctly claims zero frontend work — Prefect's pool UI lists queues natively. | None — verified against how the task-manager UI surfaces work pools. |

### Alternative Approaches

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P3 | ✅ | Simpler alternatives were considered and rejected with reasons (reusing `default` as medium; skip-if-exists provisioning; blind dispatch relying on auto-create). The chosen shape is the smallest that satisfies FR-001..008. | None. |

### Edge Cases & UX

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P4 | 💡 | Upgrade path is covered (idempotent convergence, stranded legacy runs). The **downgrade** path is not stated: an operator rolling back to a pre-priority release leaves the three queues in place and re-saves deployments without `work_queue_name`. | Add one line to the plan noting downgrade is naturally safe: old code re-saves deployments onto the pool default queue, orphaned tier queues sit empty and harmless, and workers still poll everything. No action needed at runtime — just document it. |

### Success Measurement

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P5 | ✅ | SC-001..004 are measurable and test-mapped; SC-003 ("existing suite passes unmodified") is a strong, cheap-to-verify zero-regression signal. Rollback trigger is implicit (any SC-003 failure). | None. |

---

## Engineering Lens Findings 🔬

### Architecture Soundness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | ✅ | Extends existing modules along existing seams (constants beside `WorkflowType`, provisioning beside `setup_worker_pools`, param on the existing adapter interface). Enum-as-single-source-of-truth satisfies FR-008/Constitution III. Ordering dependency (`setup_work_queues` after `setup_worker_pools`, before `setup_deployments`) is explicit in the plan. | None. |

### Failure Mode Analysis

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E2 | ✅ | The two race/failure modes that matter are analyzed with server-source evidence: queue missing at dispatch (check-first → warned fallback; auto-create closes the check-to-dispatch race) and drifted queue precedence (healed by create-or-update convergence each startup). Concurrent startup is already serialized by the `global.worker.taskmgr.init` lock. | None. |
| E3 | 💡 | The FR-006 warning is the only signal an operator gets that queue-layout drift occurred, but the plan doesn't specify which logger emits it or what it must contain — risking an unactionable or uncapturable message. | Pin it in the plan: emit via the adapter's standard structlog logger (`infrahub.log.get_logger()`), message names the missing queue, the workflow, and the fallback taken. This also makes the integration test's log assertion unambiguous. |

### Security & Privacy

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E4 | ✅ | No new trust boundaries: `priority` is an internal typed enum, never user input in this slice; no auth, no API surface, no secrets. | None. |

### Performance & Scalability

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E5 | ✅ | No-priority path is byte-for-byte today's path (zero added calls). The one extra read on the override path has no callers this slice and is negligible for its future interactive users. Queue count is 3 + default — no scaling dimension. | None. |

### Testing Strategy

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E6 | 💡 | The plan implies asserting converged precedence as absolute integers (`high=1, medium=2, low=3, default=4`). `default`'s absolute value is a side effect of Prefect's bump algorithm, not something Infrahub pins — asserting it makes the test brittle against upstream changes. | Assert absolute precedence only for the three pinned queues (1/2/3) and assert `default` only by **relative** order (precedence value greater than `low`'s). |
| E7 | ✅ | Mock-free fallback testability drove the design (D5) — the right direction of influence. Unit/integration split matches the constitution's levels; upstream waterfall behavior correctly excluded (SC-004). | None. |

### Operational Readiness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E8 | ✅ | Observability of routing comes free from the task-manager UI (queue per flow run). Rollout is plain startup convergence — no flags, no migration, restart-safe. See E3 for the one logging gap and P4 for the downgrade note. | None beyond E3/P4. |

### Dependencies & Integration

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E9 | 💡 | `infrahub-enterprise` extends the catalogue via the `build_workflows_definitions()` DI seam and may subclass `InfrahubWorkflow`. The interface change is defaulted (source-compatible), and enterprise workflows inherit `default_priority=MEDIUM` automatically — but nothing in the plan verifies this. | Add an implementation-phase checklist item: confirm infrahub-enterprise adapters/workflow definitions build cleanly against the extended interface (defaulted param + inherited field means no code change expected there). |

---

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|-------------------|------------|
| X1 | The check-first fallback (D5) is where product safety ("task execution never breaks") and engineering discipline (no-mock testability) converge — the design is better *because* the test constraint shaped it. | FR-006 guarantee holds in both race orderings | Deterministic, mock-free integration test | Keep D5 as-is; resist any "simplify to try/except" review pressure — that variant is untestable. |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 5 (P1, P4, E3, E6, E9) |
| 🤔 Questions | 0 |
| Product findings | 2 substantive (P1, P4) |
| Engineering findings | 3 substantive (E3, E6, E9) |
| Cross-lens findings | 1 (X1) |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| P1 | Product | 💡 | Problem Validation | YAGNI rationale must survive to the PR description | Explicit task in tasks.md |
| P4 | Product | 💡 | Edge Cases | Downgrade path unstated | One-line plan note: naturally safe |
| E3 | Engineering | 💡 | Failure Modes | Fallback warning logger/content unspecified | Pin logger + message content in plan |
| E6 | Engineering | 💡 | Testing | Absolute precedence assertion for `default` queue is brittle | Assert relative order for `default` |
| E9 | Engineering | 💡 | Dependencies | Enterprise compat unverified | Checklist item during implementation |

---

## Recommended Actions

### 🎯 Must-Address (Before Proceeding)

None.

### 💡 Recommendations (Strongly Suggested)

1. **P4/E3**: Amend `plan.md` — add downgrade note and pin the fallback warning's logger and content. *(Applied autonomously — low-risk doc edits.)*
2. **E6**: Amend `plan.md` testing table — relative-order assertion for the `default` queue. *(Applied autonomously.)*
3. **P1/E9**: Carry into `tasks.md` as explicit tasks (PR-description rationale; enterprise compat check). *(Handed to the tasks phase.)*

### 🤔 Questions (Need Stakeholder Input)

None.

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit.critique` — Dual-lens strategic and technical review for spec-driven development.*
