# Contracts: Unify UI Components

This feature is frontend-only with no REST/GraphQL API changes.

Component API contracts (props interfaces) are documented in [data-model.md](../data-model.md).

No OpenAPI or GraphQL schema changes are needed.
