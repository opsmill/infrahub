---
name: from-observium
source:
  name: observium
  settings:
    url: "OBSERVIUM_URL"
    # api_endpoint: "api/v0"
    # auth_method: "basic"
    username: "OBSERVIUM_USERNAME"
    password: "OBSERVIUM_TOKEN"

destination:
  name: infrahub
  settings:
    url: "http://localhost:8000"

order: [
  "IpamIPAddress",
  "InfraDevice",
]

schema_mapping:
  # Primary Device IP
  - name: IpamIPAddress
    mapping: devices
    identifiers: ["address"]
    fields:
      - name: address
        mapping: ip
      - name: description
        mapping: hostname

  - name: InfraDevice
    mapping: devices
    identifiers: ["name"]
    fields:
      - name: name
        mapping: hostname
      - name: hostname
        mapping: hostname
      - name: serial_number
        mapping: serial
      - name: description
        mapping: os
      - name: type
        mapping: hardware
