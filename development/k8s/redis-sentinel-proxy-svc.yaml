apiVersion: v1
kind: Service
metadata:
  name: redis-sentinel-proxy
  namespace: infrahub
  labels:
    app.kubernetes.io/name: redis-sentinel-proxy
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: redis
    name: cache-ha
  selector:
    app.kubernetes.io/name: redis-sentinel-proxy
