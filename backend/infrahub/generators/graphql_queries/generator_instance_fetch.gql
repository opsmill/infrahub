query GeneratorInstanceFetch($definition_id: ID, $object_id: ID) {
    CoreGeneratorInstance(definition__ids: [$definition_id], object__ids: [$object_id]) {
        edges {
            node {
                id
                status {
                    value
                }
            }
        }
    }
}
