Removing an attribute or relationship from a generic schema now also reconciles `uniqueness_constraints`, `human_friendly_id`, `display_labels`, and `order_by` on inheriting node schemas. Previously a node that inherited a constraint referencing the removed field could end up with an orphaned path, causing a `Requested unique constraint not found within node` exception.
