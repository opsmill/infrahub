---
extends: default

ignore: |
  /examples
  /repositories
  /frontend/node_modules

rules:
  new-lines: disable
  line-length:
    max: 120
    allow-non-breakable-words: true
    allow-non-breakable-inline-mappings: false
