name: Sync Folders

on:
  push:
    paths:
      - 'docs/docs/**'
      - 'docs/sidebar.ts'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          path: source-repo

      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          repository: opsmill/infrahub-docs
          token: ${{ secrets.GH_INFRAHUB_BOT_TOKEN }}
          path: target-repo

      - name: Sync folders
        run: |
          rm -rf target-repo/docs/docs/*
          rm -rf target-repo/docs/sidebar.ts
          cp -r source-repo/docs/docs/* target-repo/docs/docs/
          cp source-repo/docs/sidebar.ts target-repo/docs/sidebar.ts
          cd target-repo
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "Sync folder from source repository" && git push)
          git diff --quiet && git diff --staged --quiet || (git commit -m "Sync folder from source repository" && git push)