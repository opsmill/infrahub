on:
  pull_request:
    branches:
      - lgu-stable
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged
    runs-on: ubuntu-latest
    steps:
      - name: "Check out repository code"
        uses: "actions/checkout@v4"
        with:
          submodules: true

      - run: |
          git checkout lgu-stable
          gh pr create --title "test" --base lgu-develop
