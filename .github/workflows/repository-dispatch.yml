---
# yamllint disable rule:truthy rule:line-length
name: Trigger Infrahub update in other repositories

on:
  workflow_dispatch:
    inputs:
      runs-on:
        description: "The OS to run the job on"
        required: false
        default: "ubuntu-22.04"
        type: string
      version:
        type: string
        required: false
        description: The string to extract semver from.
        default: ''
  workflow_call:
    inputs:
      runs-on:
        description: "The OS to run the job on"
        required: false
        default: "ubuntu-22.04"
        type: string
      version:
        type: string
        required: false
        description: The string to extract semver from.
        default: ''
    secrets:
      GH_UPDATE_PACKAGE_OTTO:
        description: PAT for cross-repo repository dispatch
        required: true
      INFRAHUB_PACKER_REPOSITORY:
        description: Packer repository name
        required: false
      INFRAHUB_ENTERPRISE_REPOSITORY:
        description: Enterprise repository name
        required: false
      INFRAHUB_CUSTOMER1_REPOSITORY:
        description: Customer 1 repository name
        required: false
      INFRAHUB_CUSTOMER3_REPOSITORY:
        description: Customer 3 repository name
        required: false

permissions: {}

jobs:
  repository-dispatch:
    permissions:
      contents: read
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        # Either a literal path, or the name of a secret...
        repo:
          - "opsmill/infrahub-demo-dc"
          - "INFRAHUB_PACKER_REPOSITORY"
          - "INFRAHUB_ENTERPRISE_REPOSITORY"
          - "INFRAHUB_CUSTOMER1_REPOSITORY"
          - "INFRAHUB_CUSTOMER3_REPOSITORY"

    steps:
      - name: Resolve repository name
        id: repo
        run: |
          if echo "$MATRIX_REPO" | grep -q '/'; then
            echo "name=$MATRIX_REPO" >> "$GITHUB_OUTPUT"
          else
            echo "name=$SECRET_REPO" >> "$GITHUB_OUTPUT"
          fi
        env:
          MATRIX_REPO: ${{ matrix.repo }}
          SECRET_REPO: ${{ contains(matrix.repo, '/') && matrix.repo || secrets[matrix.repo] }}

      - name: Checkout code
        if: steps.repo.outputs.name != ''
        uses: actions/checkout@v6

      - name: Repository Dispatch
        if: steps.repo.outputs.name != ''
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.GH_UPDATE_PACKAGE_OTTO }}
          repository: ${{ steps.repo.outputs.name }}
          event-type: trigger-infrahub-update
          client-payload: |
            {"version":"${{ inputs.version }}"}
