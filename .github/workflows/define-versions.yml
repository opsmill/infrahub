---
# yamllint disable rule:truthy rule:truthy rule:line-length
name: "Define versions"
on:
  workflow_call:
    outputs:
      POETRY_VERSION:
        value: "2.1.3"

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
